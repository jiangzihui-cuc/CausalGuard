# Git 分支与协作规则

> 适用范围：隐私因果哨兵（CausalGuard）
> 团队人数：2 人（成员 A 技术实现主责，成员 B 产品与智能分析主责）

## 一、分支模型

| 分支 | 用途 | 规则 |
|---|---|---|
| `main` | 只保留可运行版本 | 禁止直接提交；只能通过已验收的 `feature/*` 合并 |
| `feature/<模块名>` | 单个功能开发 | 从最新 `main` 拉出，一个分支只解决一个明确问题 |
| `docs/<模块名>` | 文档类变更 | 文档独立成支，便于两人复核 |
| `fix/<问题名>` | 缺陷修复 | 合并前必须说明复现步骤和验证方式 |

命名示例：

```text
feature/event-model
feature/room-database
feature/usage-context
feature/vpn-observer
docs/project-charter
fix/crash-on-permission-deny
```

## 二、提交规则

1. 每次提交只解决一个明确问题。
2. 提交信息使用“动词 + 对象”的简洁描述，例如：
   - `add PrivacyEvent data model`
   - `implement mock event injector`
   - `docs: freeze P0 scope`
3. 规则、数据模型和文档变更必须与对应代码处于同一提交或同一版本记录。
4. 不提交真实隐私数据、密钥、个人账号和设备日志原件。
5. 第三方代码必须单独记录来源、版本和许可证。

## 三、合并与复核

1. 成员 A 提交代码后，由成员 B 按用户流程体验；成员 B 修改文案或规则后，由成员 A 检查是否能落地实现。
2. 合并到 `main` 前必须满足：
   - 能编译或文档自洽；
   - 不破坏演示主流程；
   - 所有新结论都能追溯到证据；
   - 真实模式与沙箱模式标识清晰。
3. 任何新增功能必须先修改 `docs/04-product-requirements.md`、数据模型和验收标准，不能直接进入代码。

## 四、版本与一致性

- 文档基线版本统一标记为 `v0.1`，冻结后再递增。
- APK、源代码、设计文档、截图和视频展示的必须是同一版本功能。
- 版本号规则：`主版本.次版本.修订号`，如 `0.1.0`。

## 五、每日工作机制

- 每日开始前：确认当天唯一最重要的交付目标、需要的接口或数据、需先验证的技术假设。
- 每日结束前：更新任务看板（`docs/17-task-board.md`）状态、保存截图/日志、记录技术边界、生成一个可复现版本。
- 每周至少一次 30 分钟进度会议，确认已完成、进行中和阻塞事项。
